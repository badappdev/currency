
run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - cyclop
    - goimports
    - golint
    - govet
    - staticcheck
    - gosec

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0